# Animation improvement plans

Generated by `/improve-animations` against commit `de894c7`. Covers `frontend_sj/frontend` (React/Tailwind) and `mobile_sj` (Flutter). Each plan is self-contained — see [PLAN-TEMPLATE.md](../../.claude/skills/improve-animations/PLAN-TEMPLATE.md) format. Execute with `improve-animations execute plans/NNN-*.md`, or hand a plan file directly to any agent/model.

## Plans

| # | Title | Severity | Category | App | Status |
| --- | --- | --- | --- | --- | --- |
| [001](001-toast-interruptible-exit.md) | Make toast enter/exit interruptible | HIGH | Interruptibility | frontend | DONE |
| [002](002-modal-exit-animation.md) | Add a real exit animation to the shared Modal component | HIGH | Interruptibility / Physicality | frontend | DONE |
| [003](003-gpu-only-transitions.md) | Replace `transition-all` on layout properties with GPU-only transitions | MEDIUM | Performance | frontend | DONE* |
| [004](004-asset-drawer-animation-parity.md) | Give AssetDrawer the same animation as its sibling drawers | MEDIUM | Cohesion | frontend | DONE |
| [005](005-drawer-close-transitionend.md) | Replace hand-timed `setTimeout` drawer closes with `transitionend`-driven state | MEDIUM | Interruptibility | frontend | DONE |
| [006](006-ai-reveal-animated-switcher.md) | Animate the AI recommendation/summary reveal | MEDIUM | Missed opportunity | mobile | DONE |
| [007](007-reports-list-stagger.md) | Add the missing entrance stagger to the Reports list | MEDIUM | Cohesion & tokens | mobile | DONE |
| [008](008-reduced-motion-scope.md) | Scope `prefers-reduced-motion` to movement, not all transitions | LOW-MEDIUM | Accessibility | frontend | DONE |
| [009](009-force-change-password-transition-cohesion.md) | Push ForceChangePasswordScreen with the app's own transition | LOW | Cohesion | mobile | DONE |
| [010](010-sidebar-tooltip-origin-hover-gating.md) | Fix collapsed-sidebar tooltip origin + hover gating | LOW | Physicality / Accessibility | frontend | DONE |
| [011](011-easing-token-consolidation.md) | Consolidate two near-identical cubic-beziers into named tokens | LOW | Cohesion & tokens | frontend | DONE |
| [012](012-idle-warning-modal-animation.md) | Give the idle-session warning a real entrance | LOW | Cohesion | frontend | DONE |
| [013](013-destructive-action-haptics.md) | Add haptic feedback to destructive/security confirmations | LOW-MEDIUM | Missed opportunity | mobile | DONE |
| [014](014-form-reveal-animations.md) | Animate conditional form-field reveals | LOW | Missed opportunity | mobile | DONE |
| [015](015-offline-banner-token-consolidation.md) | Swap OfflineBanner's hand-typed duration/curve for shared tokens | LOW | Cohesion & tokens | mobile | DONE |
| [016](016-report-export-success-feedback.md) | Confirm a successful report export | LOW | Missed opportunity | mobile | DONE |
| [017](017-shimmer-linear-curve.md) | Use `Curves.linear` for the looping Shimmer pulse | LOW | Easing & duration | mobile | DONE |
| [018](018-filter-badge-feedback.md) | Animate the filter-active badge | LOW | Missed opportunity | mobile | DONE* |
| [019](019-custom-motion-curve.md) | Replace the shared `motionCurve` builtin with a strong custom curve | LOW | Easing & duration / Cohesion | mobile | DONE |
| [020](020-reduced-motion-accommodation.md) | Add reduced-motion accommodation to key animated surfaces | LOW | Accessibility | mobile | DONE* |
| [021](021-stagger-delay-band.md) | Bump the stagger delay into the recommended 30-80ms band | LOW | Cohesion & tokens | mobile | DONE |

All 21 executed directly against the working tree (not a separate worktree — see note below). `flutter analyze` (mobile_sj) and `npm run build` (frontend) are both clean.

\* **003** — only 3 of the 5 originally-cited sites fit the plan's `width→scaleX` conversion (the 3 single-bar-in-track cases at lines 411/449/641). The segmented condition-distribution bar (~line 263, several adjacent divs sized by direct width %) and the day-count bar chart (~line 536, pixel-height driven, not a ratio-of-container-width) don't fit that conversion without a structural rework that risked a visible regression — left as-is per the plan's own escape hatch, noted here as follow-up candidates.

\* **018** — `Badge.isLabelVisible` has no internal transition (`if (!isLabelVisible) return child` — a hard conditional, confirmed by reading the Flutter SDK source), so the plan's literal `label:`-wrapping approach doesn't work. Implemented via the plan's own escape hatch instead: the whole `Badge` is wrapped in an `AnimatedSwitcher` (keyed by `filtersActive`, scale+fade transition) in all three list screens.

\* **020** — the two required surfaces (page transitions, staggered entrances) are done. The optional dashboard count-up/ring stretch goal was skipped, per the plan's own "skip if it doesn't feel comfortable to verify" allowance, to keep pace across all 21 plans in one pass.

## Recommended execution order

1. **001, 002** (frontend, HIGH) — highest-leverage fixes, independent of everything else. Do these first.
2. **003, 010** (frontend, same file: `Sidebar.jsx`) — execute sequentially, not in parallel, to avoid two agents patching the same file at once. Either order is fine; they touch disjoint sections (003: the `aside` width transition; 010: the collapsed tooltip).
3. **004, then 005** (frontend, drawer patterns) — do 004 first. 005 adds an `onExitAnimationEnd` prop to `RecordDrawer.jsx`/`LogDrawer.jsx`; 004 uses those same two files as a **read-only exemplar** to copy the (current, `setTimeout`-based) pattern onto `AssetDrawer.jsx`. If 005 runs first, the exemplar will already look different from what plan 004 documents — 004's executor should still succeed (the copied pattern would just include the `transitionend` improvement too), but running 004 first keeps each plan's diff attributable to itself.
4. **006, 007** (mobile, MEDIUM) — independent of each other and of everything above.
5. **008** (frontend, accessibility) — independent, low risk.
6. **009, 011, 012** (low-severity, independent files) — any order.
7. **013, 014, 015, 016, 017, 018** (mobile, LOW/polish, mostly independent files) — any order. Note 018 has a built-in escape hatch (skip if the `Badge` API doesn't cooperate) — don't block on it.
8. **021, then 020** (mobile, same file: `staggered_entrance.dart`) — **do 021 first.** Plan 020's Target code for this file still shows the pre-021 `25 *` literal (since it was written before 021's bump was decided) — if 020 is executed first instead, its executor should adapt to whatever the live literal actually is (`25` or already `40`) rather than treating the mismatch as drift-worth-stopping-over; but doing 021 first avoids the ambiguity entirely.
9. **019** (mobile, wide blast radius) — do this **last**, and in isolation from the same-session app_theme.dart changes in step 8's plan 020 (which adds a `ReducedMotionX` extension to the same file, immediately after the line 019 changes). Execute 020 before 019 if both are in play, since 019's single-line change is trivial to reapply after 020's addition; doing it in the other order risks a stale line-number reference. Feel-check 019 on its own before combining with anything else, since it affects every animated surface in the app simultaneously.

## Dependencies summary

- **003 ↔ 010**: same file (`Sidebar.jsx`), disjoint sections — sequence, don't parallelize.
- **004 → 005**: 004 should land first (or be executed with awareness that its exemplar may already be updated by 005).
- **020 ↔ 021**: same file (`staggered_entrance.dart`) — do **021 before 020**.
- **019 ↔ 020**: same file (`app_theme.dart`), adjacent lines — do **020 before 019**.
- Everything else is independent and can be executed in any order or in parallel across different files.
